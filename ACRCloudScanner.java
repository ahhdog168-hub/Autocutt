public class ACRCloudScanner {
    public static List<int[]> scan(File audioFile) {
        // Replace with real API integration
        List<int[]> list = new ArrayList<>();
        list.add(new int[]{5, 15});
        return list;
    }
}
